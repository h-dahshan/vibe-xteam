import { reactConfig } from "./base.react.js";

/**
 * A custom ESLint configuration for libs that use React.
 *
 * @type {import("eslint").Linter.Config[]} */
export const reactLibConfig = [...reactConfig];
